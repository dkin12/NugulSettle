spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nugulsettle?allowPublicKeyRetrieval=true&useSSL=true
    username: nugulsettle
    password: nugul11638070!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect